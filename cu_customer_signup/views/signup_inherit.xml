<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="auth_signup.signup" name="Sign up login">
        <t t-call="web.login_layout">
            <form class="oe_signup_form" role="form" method="post" t-if="not message" id="multiStepForm">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token" />
                <div class="container">
                    <h2 class="text-center">Zoon Account SignUp</h2>

                    <!-- Step 1: Contact Details -->
                    <div class="step step-1">
                        <h4 class="section-title">Contact Details</h4>
                        <div class="row">
                            <div class="mb-3">
                                <label for="name" class="col-form-label">First Name</label>
                                <input type="text" name="name" id="name" class="form-field" placeholder="John" required="required" />
                            </div>
                            <div class="mb-3">
                                <label for="last_name" class="col-form-label">Last Name</label>
                                <input type="text" name="last_name" id="last_name" class="form-field" placeholder="Doe" required="required" />
                            </div>
                            <div class="mb-3">
                                <label for="login">Email</label>
                                <input type="email" name="login" id="login" class="form-field" placeholder="example@mail.com" required="required" />
                            </div>
                            <div class="mb-3">
                                <label for="mobile">Mobile</label>
                                <input type="tel" name="mobile" id="mobile" class="form-field" required="required" />
                            </div>
                            <div class="mb-3">
                                <label for="dob">D.O.B</label>
                                <input type="date" name="dob" id="dob" class="form-field" required="required" />
                            </div>
                        </div>
                    </div>

                    <!-- Step 2: Address -->
                    <div class="step step-2 d-none">
                        <h4 class="section-title">Address</h4>
                        <div class="mb-3">
                            <label for="street1">Street 1</label>
                            <input type="text" name="street1" id="street1" class="form-field" required="required" />
                        </div>
                        <div class="mb-3">
                            <label for="street2">Street 2</label>
                            <input type="text" name="street2" id="street2" class="form-field" />
                        </div>
                        <div class="mb-3">
                            <label for="city">City</label>
                            <input type="text" name="city" id="city" class="form-field" required="required" />
                        </div>
                        <div class="mb-3">
                            <label for="country">Country</label>
                            <select name="country" id="country" class="form-field" required="required">
                                <option value="">Select a country</option>
                                <t t-foreach="countries" t-as="country">
                                    <option t-att-value="country.id" t-esc="country.name" />
                                </t>
                            </select>
                        </div>
                    </div>

                    <!-- Step 3: Miscellaneous -->
                    <div class="step step-3 d-none">
                        <h4 class="section-title">Miscellaneous</h4>
                        <div class="mb-3">
                            <label for="registration_method">Registration Method</label>
                            <select name="registration_method" id="registration_method" class="form-field" required="required">
                                <option value="">Select a method</option>
                                <option value="Amazonia Mall">Amazonia Mall</option>
                                <option value="West Central Mall">West Central Mall</option>
                                <option value="Giftland">Giftland</option>
                                <option value="Main Branch">Main Branch</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="source">How did you find out about us?</label>
                            <select name="source" id="source" class="form-field" required="required">
                                <option value="">Select an option</option>
                                <option value="TV Advertisements">TV Advertisements</option>
                                <option value="Referral">Referral</option>
                                <option value="Search Engine">Search Engine</option>
                                <option value="Verbal">Verbal</option>
                                <option value="Other">Other</option>
                            </select>
                        </div>
                    </div>

                    <!-- Step 4: Account Specifics -->
                    <div class="step step-4 d-none">
                        <h4 class="section-title">Account Specifics</h4>
                        <div class="mb-3">
                            <label for="account_type">Account Type</label>
                            <select name="account_type" id="account_type" class="form-field" required="required">
                                <option value="Zoon">Personal</option>
                                <option value="Zoon+">Zoon+</option>
                                <option value="Staff">Staff</option>
                                <option value="Business">Business</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="password">Password</label>
                            <input type="password" name="password" id="password" class="form-field" required="required" />
                        </div>
                        <div class="mb-3">
                            <label for="confirm_password">Confirm Password</label>
                            <input type="password" name="confirm_password" id="confirm_password" class="form-field" required="required" />
                        </div>
                    </div>

                    <!-- Navigation Buttons -->
                    <div class="navigation-buttons">
                        <button type="button" class="btn btn-secondary prev d-none">Previous</button>
                        <button type="button" class="btn btn-primary next">Next</button>
                        <button type="submit" class="btn btn-success submit d-none">Submit</button>
                    </div>
                </div>
            </form>
        </t>
    </template>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const steps = document.querySelectorAll('.step');
            const nextBtn = document.querySelector('.next');
            const prevBtn = document.querySelector('.prev');
            const submitBtn = document.querySelector('.submit');
            let currentStep = 0;

            const updateSteps = () => {
                steps.forEach((step, index) => {
                    step.classList.toggle('d-none', index !== currentStep);
                });
                prevBtn.classList.toggle('d-none', currentStep === 0);
                nextBtn.classList.toggle('d-none', currentStep === steps.length - 1);
                submitBtn.classList.toggle('d-none', currentStep !== steps.length - 1);
            };

            nextBtn.addEventListener('click', () => {
                if (currentStep < steps.length - 1) currentStep++;
                updateSteps();
            });

            prevBtn.addEventListener('click', () => {
                if (currentStep > 0) currentStep--;
                updateSteps();
            });

            updateSteps();
        });
    </script>
</odoo>
